# tree-sitter-http-request

A minimal Tree-sitter grammar for IntelliJ-style HTTP request files used by the `zed-http-client` Zed extension.

This grammar is intentionally lightweight in the initial repository. The Rust parser remains the source of truth for execution semantics.
